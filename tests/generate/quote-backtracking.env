# The way the regular expressions of the original work is that it does allow a
# backslash before the ending quote if there are no more other quotes in the file.

FOO = "  hello
world  \"  # comment
BAR = not quoted
